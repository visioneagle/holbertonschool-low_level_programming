#include <stdio.h>

/*
 * Main: déclare une variable type char puis initialise la premmère lettre de l'alphabet
 * Return: Success
 */

int main(void)
{
	char alphabet = 'a';

	while ( alphabet <= 'z');
	{	

		putchar (alphabet);

		alphabet++;

	}	
	putchar('\n')
		return (0);

}
